#!/bin/sh

####################################### ✓ isuser: exported function
# Is user (not root and not sudo)?
# Arguments:
#   --usage             Show help message and exit.
# Output:
#   Help message to stdout.
# Returns:
#   1 if no root and not sudo and --usage and not man page
#######################################
usage "${@}" || exit
! isroot "" && ! issudo ""

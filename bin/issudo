#!/bin/sh

####################################### ✓ issudo: exported function
# Is sudo (SUDO_UID is set)?
# Arguments:
#   --usage             Show help message and exit.
# Output:
#   Help message to stdout.
# Returns:
#   1 if no 'SUDO_UID' and --usage and not man page.
#######################################
usage "${@}" || exit
[ "${SUDO_UID-}" ]

#!/bin/sh

# Manages rc.d repository, system profile $RC_GENERATED generated files
#
set -eu

paths() {

}

main() {
#  if test "$#" -eq 0 || { test "${1-}" = '--parsed' && shift; }; then
    force=false; [ "${RC_PROFILE-0}" -eq 1 ] || force=true
    for arg do
      case "${arg}" in
        --force) force=true ;;
      esac
    done
#  else
#    PARSE="${0}" parse "$@"
#  fi
}
main "$@"

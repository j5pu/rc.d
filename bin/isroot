#!/bin/sh


####################################### ✓ isroot: exported function
# Is root (id -u is 0)?
# Arguments:
#   --usage             Show help message and exit.
# Output:
#   Help message to stdout.
# Returns:
#   1 if no root and --usage and not man page.
#######################################
usage "${@}" || exit
[ "$(id -u)" -eq 0 ]

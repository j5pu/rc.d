#!/bin/bash

#######################################
# Show colors
#######################################
usage "${@}" || exit
[ "${BASH_VERSION-}" ] || exit
for i in ${COLORS}; do
  printf "%b\n" "${!i}${i}${Reset}"
done
inf Info message
warn Warning message
err Error message
false || die Die with error message

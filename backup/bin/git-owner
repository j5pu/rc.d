#!/bin/sh

# Git repo owner
#
set -eu; PATH="$(dirname "${0}"):${PATH}" && . strict.lib

for arg do
  case "${arg}" in
    -h|--help) usage "${0}" ;;
    --desc) desc "${0}" ;;
    --version) rc.d.version ;;
  esac
done

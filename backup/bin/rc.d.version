#!/bin/sh

# rc.d repository version
#
set -eu; PATH="$(dirname "${0}"):${PATH}" && . strict.lib

version='0.0.1'

for arg do
  case "${arg}" in
    -h|--help) usage "${0}" ;;
    --desc) desc "${0}" ;;
    --version) echo "${version}"; exit;;
  esac
done

echo "${version}"
